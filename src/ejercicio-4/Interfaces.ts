/**
 * Interfaz que define el método print.
 * @property print Método para imprimir.
 */
export interface Printable {
  print(): void;
}

/**
 * Interfaz que define el método scan.
 * @property scan Método para escanear.
 */
export interface Scannable {
  scan(): void;
}
